import type { Theme } from '@webetnes/core';

/** Palette Merge Drop : prune profonde, bac lavande, accents crème. */
export const theme: Theme = {
  background: 0x2b1d3a,
  surface: 0x3f2d55,
  primary: 0xffd166,
  primaryText: '#2b1d3a',
  text: '#fbf5ff',
  textMuted: '#b9a7d1',
  accent: 0x8be04b,
  danger: 0xff5d73,
};

/** Couleur des murs et du sol du bac. */
export const BIN_COLOR = 0x5a4478;
/** Couleur de la ligne de danger au repos. */
export const LINE_COLOR = 0x8f7bb0;
