/** Constantes de jeu partagées par la logique et les scènes (sans dépendance Phaser). */
export const BASE_WIDTH = 360;
export const BLOCK_HEIGHT = 64;
/** Décalage maximal (px) pour un placement parfait. */
export const PERFECT_THRESHOLD = 8;
/** Marge latérale que le bloc mobile ne dépasse pas. */
export const SIDE_MARGIN = 16;
/** Largeur minimale du bloc mobile offert après une pub récompensée. */
export const CONTINUE_MIN_WIDTH = 200;
/** Ordonnée écran vers laquelle la caméra ramène le sommet de la tour. */
export const TOP_TARGET_Y = 720;
/** Tous les N perfects consécutifs, le son « combo » remplace le son « perfect ». */
export const COMBO_EVERY = 5;
